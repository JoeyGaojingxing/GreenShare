<!--pages/userlist/userlist.wxml-->
<view class="root">
    <view class="avatar" wx:for="{{users}}">
        <l-avatar
            size='120'
            src="{{item.avatar}}"
            text="{{item.nickname}}" />
 
        <view >{{item.signature}}</view>
    </view>
</view>